# 우리들의 학교축제 대작전! — 개발 및 기술 문서

> Unity와 C#으로 제작한 카드 · 자원 관리 · Hex Tile 기반 전략 게임의 개발 기록입니다.

이 문서에서는 단순히 사용한 기술을 나열하기보다  
프로젝트를 구현하면서 **어떤 문제가 있었고, 어떤 구조를 선택했으며, 현재 구조에서 무엇을 개선할 수 있는지**를 중심으로 설명합니다.

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [개발 환경](#2-개발-환경)
3. [주요 런타임 구조](#3-주요-런타임-구조)
4. [Card System](#4-card-system)
5. [Hex Grid & Cube Coordinate System](#5-hex-grid--cube-coordinate-system)
6. [Tile System](#6-tile-system)
7. [Tech System](#7-tech-system)
8. [Snapshot 기반 Undo](#8-snapshot-기반-undo)
9. [Save / Load](#9-save--load)
10. [GameManager](#10-gamemanager)
11. [구현 과정에서 중요하게 생각한 점](#11-구현-과정에서-중요하게-생각한-점)
12. [현재 구조에서 개선하고 싶은 부분](#12-현재-구조에서-개선하고-싶은-부분)
13. [개발 과정](#13-개발-과정)
14. [프로젝트를 통해 얻은 경험](#14-프로젝트를-통해-얻은-경험)
15. [마무리](#15-마무리)

## 1. 프로젝트 개요

**우리들의 학교축제 대작전!**은 제한된 15일 동안 자원을 관리하며 카드를 사용하고, 학교 부지에 시설을 배치해 축제를 준비하는 전략 게임입니다.

게임의 주요 시스템은 서로 독립적으로 동작하지 않고, 다음과 같은 흐름으로 연결됩니다.

```text
Card Draw
    ↓
Resource Management
    ↓
Card Execution
    ↓
Tech / Festival Status Update
    ↓
Tile Construction
    ↓
Tile Action / Score Calculation
    ↓
Turn Progression
    ↓
Ending / Result

Supporting Systems
├─ Save / Load
└─ Undo
```

이 프로젝트에서 가장 중요하게 생각한 목표는  
**각 기능을 개별적으로 구현하는 데 그치지 않고 실제 플레이 가능한 하나의 게임 루프로 연결하는 것**이었습니다.

## 2. 개발 환경

| 항목 | 내용 |
| --- | --- |
| 개발 형태 | 개인 개발 |
| 초기 개발 기간 | 15일 |
| Engine | Unity 2022.3.62f3 |
| Language | C# |
| UI | Unity UI / TextMeshPro |
| Version Control | Git / GitHub |
| Target Platform | Windows |

## 3. 주요 런타임 구조

```text
Assets/
└─ Scripts/
   └─ Runtime/
      ├─ Card/
      │  ├─ 카드 데이터
      │  ├─ 카드 표시 및 실행
      │  └─ 손패 관리
      │
      ├─ Dialogue/
      │  └─ 대화 및 스토리 진행
      │
      ├─ GameManager/
      │  ├─ 게임 상태 관리
      │  ├─ 액션 처리
      │  ├─ 일일 이벤트
      │  ├─ 타일 관리
      │  ├─ 턴 진행
      │  ├─ UI 관리
      │  └─ Undo (실행 취소)
      │
      ├─ Save/
      │  └─ 저장 / 불러오기 및 세이브 슬롯
      │
      ├─ Tiles/
      │  └─ 육각형 타일 및 시설별 동작
      │
      ├─ SceneManager/
      │  └─ 씬 전환
      │
      └─ Tooltip/
         └─ 툴팁 UI
```

## 4. Card System

### 문제

게임 특성상 카드의 종류가 계속 증가합니다.

카드마다 새로운 C# 클래스를 작성하는 방식으로 구현하면

- 카드 추가 비용이 높아지고
- 밸런스 수치 변경에도 코드 수정이 필요하며
- 카드 데이터와 실행 로직이 서로 강하게 결합될 가능성이 높아집니다.

따라서 카드의 정적인 데이터와 실제 실행 로직을 분리하고,  
새로운 카드를 추가할 때 기존 코드를 최대한 재사용할 수 있는 구조가 필요했습니다.

### 선택

카드의 정적인 데이터는 Unity의 `ScriptableObject`로 관리했습니다.

카드 데이터는 대표적으로 다음과 같은 정보를 가지고 있습니다.

```text
GameCard
├─ CardID                : 카드의 고유 ID
├─ CardName              : 카드 이름
├─ Description           : 카드 설명
├─ Tooltip               : 카드 내 용어 설명
├─ Cost                  : 카드 사용 비용
├─ TagList               : 카드 사용에 필요한 기술 조건
├─ ActionList            : 카드 사용 시 실행할 효과 정보
├─ AddCardOnHand         : 카드 사용 시 손에 추가되는 카드
└─ AddCardOnDeck         : 카드 사용 시 덱에 추가되는 카드
```

관련 코드:

```text
Assets/Scripts/Runtime/Card/GameCard.cs
Assets/Scripts/Runtime/Card/
```

### 구현 방식

`GameCard`는 카드의 데이터와 실행할 효과 정보를 정의하고,  
실제 게임에서 동작하는 `CCard`가 해당 정보를 읽어 실행 가능한 런타임 액션으로 변환합니다.

카드 사용 시 실행되는 효과는 `Func<>` 형태의 콜백으로 등록되며,  
`CCard.UseCard()`가 호출되면 등록된 액션들을 순서대로 실행합니다.

```text
GameCard
   │
   │  카드 데이터 / 효과 정보
   ↓
CCard
   │
   │  실행 가능한 Func<> 등록
   ↓
CCard.UseCard()
   │
   └─ 등록된 효과 순차 실행
```

자원 획득, 생산량 변경, 카드 드로우처럼 여러 카드에서 반복되는 동작은  
`CCardStatic`의 공통 효과로 분리해 재사용합니다.

효과의 성격에 따라 런타임 동작을 연결하는 방식은 조금씩 다르게 구성했습니다.

#### 값에 따라 동작하는 효과

카드 점수처럼 하나의 값으로 효과의 종류와 크기를 표현할 수 있는 경우에는  
해당 값을 해석하는 콜백을 생성해 등록합니다.

예를 들어 카드 점수는 `FindPointFunction()`에서 전달받은 값을 해석해  
점수와 해당 분야를 결정하는 `Func<GameManager, SScoreInfo>`를 반환합니다.

```text
level
  ↓
FindPointFunction(level)
  ↓
점수 값 / 분야 해석
  ↓
Func<GameManager, SScoreInfo>
  ↓
카드의 점수 계산 시 실행
```

이를 통해 공용, 과학, 음악, 미술, 체육, 미스터리처럼  
동일한 점수 계산 구조를 사용하는 효과를 하나의 콜백 생성 함수로 처리할 수 있도록 했습니다.

#### 카드별 특수 효과

반대로 기존 공통 효과의 조합만으로 표현하기 어려운 카드에는  
별도의 특수 효과 함수를 구현했습니다.

이 경우 `CardCustom()`에서 효과 식별값을 `switch` 문으로 확인하고,  
해당 값과 연결된 실제 함수를 반환하도록 구성했습니다.

```text
Custom Effect ID
       ↓
CardCustom(level)
       ↓
    switch
       ↓
CardCustom01 / CardCustom02 / ...
       ↓
  Func<GameManager, int, bool>
```

예를 들어 특정 시설의 개수를 확인한 뒤 조건을 만족하면 생산량을 증가시키는 것처럼,  
해당 카드만의 게임 상태 검사나 복합적인 처리가 필요한 경우 이 방식을 사용합니다.

따라서 카드 시스템은 모든 효과를 하나의 형태로 강제하기보다,

- 반복적으로 사용되는 효과는 `CCardStatic`의 공통 Action으로 재사용하고
- 값으로 표현할 수 있는 효과는 해당 값을 해석하는 콜백을 생성하며
- 공통 구조로 표현하기 어려운 특수 효과는 식별값과 `switch`를 통해 전용 함수에 연결하는

방식으로 구성했습니다.

개발 초기에는 일부 카드 효과가 개별적으로 구현되어 있었지만,  
카드 수가 증가하면서 반복되는 동작은 가능한 범위에서 공통 Action으로 통합했습니다.

이를 통해 새로운 카드를 추가할 때 새로운 클래스를 계속 만드는 대신  
기존 효과를 조합하거나, 필요한 경우에만 특수 효과를 추가할 수 있도록 했습니다.

### 장점

카드의 이름, 비용, 설명과 같은 데이터는 Unity Editor에서 수정할 수 있기 때문에  
단순한 밸런스 변경을 위해 매번 C# 코드를 수정할 필요가 줄었습니다.

또한 카드의 정적인 데이터와 화면에 표시되는 런타임 카드 객체를 분리함으로써  
카드 데이터의 관리와 실제 게임 동작의 책임을 구분할 수 있었습니다.

공통 효과를 재사용하는 구조를 통해 카드 수가 늘어날수록 발생할 수 있는  
중복 구현도 줄일 수 있었습니다.

### 프로젝트에서 배운 점

초기 기획 단계에서는 카드별로 별도의 클래스를 두는 구조도 고려했지만,  
실제 구현에 들어가기 전에 카드 수가 지속적으로 증가할 것을 예상했습니다.

카드마다 클래스를 추가하는 방식은 처음에는 단순하지만, 콘텐츠가 늘어날수록 비슷한 데이터와 동작이 반복되고 관리해야 할 코드도 함께 증가할 수 있다고 판단했습니다.

그래서 실제 개발을 시작하기 전에 카드의 정적인 정보는 `ScriptableObject`로 관리하고, 반복되는 동작은 공통 Action으로 재사용하는 방향으로 구조를 변경했습니다.

이 경험을 통해 **카드처럼 지속적으로 콘텐츠가 추가되는 시스템에서는, 구현을 시작하기 전에 반복될 데이터와 동작을 미리 구분하고 확장 가능한 구조를 잡는 것이 중요하다**는 점을 배웠습니다.

다만 개발을 진행하면서 모든 카드 효과를 동일한 형태로 공통화할 수 있는 것은 아니라는 점도 확인했습니다.

자원 획득이나 생산량 변경처럼 여러 카드에서 반복되는 효과는 공통 Action으로 재사용하기 적합했지만, 특정 타일의 수를 검사하거나 게임 상태에 따라 서로 다른 결과를 만들어내는 효과는 카드 고유의 로직이 필요했습니다.

따라서 이 프로젝트에서는

- 카드의 정적인 정보는 데이터로 관리하고
- 반복되는 효과는 공통 Action으로 재사용하며
- 값에 따라 달라지는 효과는 콜백을 통해 처리하고
- 공통 구조로 표현하기 어려운 효과는 별도의 함수로 구현하는

방식을 사용했습니다.

이를 통해 **공통화할 수 있는 기능은 재사용 가능한 구조로 만들고, 예외적인 동작은 별도로 처리하는 것이 더 유연한 설계가 될 수 있다**는 점을 배웠습니다.

## 5. Hex Grid & Cube Coordinate System

### 문제

게임에서는 시설을 육각형 타일 위에 배치하며, 여러 시설이 주변 타일과 상호작용합니다.

따라서 단순히 화면 위에 타일을 배치하는 것뿐 아니라

- 특정 타일의 6방향 인접 타일 탐색
- 일정 범위 내의 타일 탐색
- 시설 간 위치 관계 확인
- 주변 시설을 이용한 점수 계산

등을 반복적으로 처리할 필요가 있었습니다.

육각형 타일은 사각형 격자와 달리 행과 열만으로 인접 관계를 처리하면 방향에 따라 계산 방식이 달라질 수 있기 때문에, 타일의 위치와 주변 관계를 일관되게 표현할 좌표계가 필요했습니다.

### 선택

Hex Grid의 논리 좌표를 표현하기 위해 **Cube Coordinate System**을 사용했습니다.

Cube Coordinate는 세 개의 축을 사용하지만

```text
x + y + z = 0
```

이라는 제약을 가지므로 실제 자유도는 2차원과 같습니다.

육각형의 여섯 방향을 세 축을 기준으로 대칭적으로 표현할 수 있어, 인접 타일이나 범위 탐색과 같은 Hex Grid 연산을 비교적 일관된 방식으로 처리할 수 있습니다.

### 구현 방식

각 타일의 위치를 Cube Coordinate 기준으로 관리하고,  
육각형의 여섯 방향을 나타내는 좌표 변화량을 이용해 주변 타일을 탐색하도록 구성했습니다.

```text
            Neighbor
               ↑
        Neighbor   Neighbor
             \     /
              Tile
             /     \
        Neighbor   Neighbor
               ↓
            Neighbor
```

이 좌표 정보를 바탕으로 게임에서는 주변 타일을 확인하고,  
시설별 점수 계산이나 배치 조건 등 타일 간 관계가 필요한 로직에서 활용합니다.

예를 들어 주변에 특정 종류의 시설이 존재하는지 확인하거나,  
지정된 범위 안에 있는 타일을 탐색해야 하는 경우 같은 좌표 탐색 로직을 재사용할 수 있습니다.

#### BFS를 이용한 연결된 시설 탐색

음악 동아리의 버스킹 부스는 음향 중계기가 연속해서 연결되어 있을수록  
더 높은 점수를 얻는 구조로 구현했습니다.

이 경우 버스킹 부스에 바로 인접한 타일만 확인하는 것이 아니라,  
인접한 음향 중계기를 따라가며 그 중계기에 연결된 다른 음향 중계기까지 계속 탐색해야 합니다.

이를 위해 `Queue`와 `HashSet`을 이용한 **BFS 방식의 연결 탐색**을 사용했습니다.

```text
버스킹 부스 위치
      ↓
Queue에 시작 위치 추가
      ↓
현재 위치 Dequeue
      ↓
6방향 인접 타일 탐색
      ↓
방문하지 않은 음향 중계기 발견
      ↓
HashSet에 기록 + Queue에 추가
      ↓
Queue가 빌 때까지 반복
```

`HashSet`으로 이미 확인한 좌표를 관리해 같은 타일을 반복해서 탐색하는 것을 방지하고,  
`Queue`를 이용해 새로 발견한 음향 중계기를 순서대로 탐색하도록 구성했습니다.

이를 통해 버스킹 부스와 직접 인접하지 않더라도  
다른 음향 중계기를 통해 이어져 있는 **전체 음향 중계기 네트워크의 크기**를 점수 계산에 사용할 수 있었습니다.

관련 코드:

```text
Assets/Scripts/Runtime/Tiles/TileBuskingBooth.cs
```

### 장점

시설마다 주변 타일을 찾는 방법을 별도로 구현하지 않고  
Hex Grid의 좌표와 탐색 방식을 공통으로 사용할 수 있었습니다.

단순히 인접한 6개 타일뿐 아니라 **반경 2 이상의 범위에 있는 타일을 탐색해야 하는 기능**이 있었기 때문에, Cube Coordinate를 이용해 거리와 범위를 일관된 방식으로 처리할 수 있었습니다.

또한 게임 화면상의 위치와 별개로 타일의 논리적인 위치 관계를 좌표로 관리하기 때문에, 시설별 로직에서는 실제 World Position을 직접 비교하지 않고 **타일 간의 거리와 위치 관계를 기준으로 게임 규칙을 구현할 수 있었습니다.**

### 프로젝트에서 배운 점

육각형 타일에서 바로 인접한 6개 타일만 필요했다면, 각 타일이 자신의 이웃을 직접 참조하도록 구현하는 방법도 충분히 가능했습니다.

하지만 이 프로젝트에서는 인접 타일뿐 아니라 **반경 2 이상의 타일을 탐색하고, 해당 범위 안의 시설을 확인하는 기능**이 필요했습니다.

이웃 타일의 참조만으로도 반복 탐색을 통해 구현할 수 있지만, 탐색 범위가 넓어질수록 중복 방문 처리와 거리 판단이 추가로 필요합니다. 그래서 타일의 위치와 거리를 일관된 방식으로 표현할 수 있는 Cube Coordinate System을 선택했습니다.

이를 통해 **같은 기능이라도 실제로 필요한 연산이 무엇인지에 따라 적합한 데이터 표현 방식이 달라질 수 있다**는 점을 배웠습니다.

또한 구현 방법을 선택할 때 단순히 익숙하거나 구현하기 쉬운 방식을 사용하는 것보다, 이후 게임 규칙에서 어떤 연산이 반복적으로 필요한지를 먼저 고려하는 것이 중요하다는 점을 경험했습니다.

## 6. Tile System

### 문제

게임에는 다양한 시설이 존재하며 시설마다 서로 다른 규칙을 가지고 있습니다.

예를 들어 어떤 시설은

- 주변 타일의 종류나 개수를 확인해 점수를 계산하고
- 플레이어가 직접 사용할 수 있는 Tile Action을 가지며
- 플레이어의 기술 상태에 따라 효과가 강화되거나
- 자신만의 별도 점수 계산 방식을 가집니다.

시설이 추가될 때마다 이러한 규칙을 `GameManager`의 `switch` 또는 `if` 문에 계속 추가하면, 시설의 수가 늘어날수록 하나의 코드가 여러 시설의 규칙을 모두 알아야 하는 구조가 됩니다.

따라서 시설의 공통 동작은 공유하면서도  
각 시설이 자신의 고유한 규칙을 직접 처리할 수 있는 구조가 필요했습니다.

### 선택

공통 타일 클래스인 `CTile`을 만들고,  
각 시설이 이를 상속하도록 구성했습니다.

```text
CTile
├─ TileArtMuseum
├─ TileBuskingBooth
├─ TileFoodbooth
├─ TileCultBooth
├─ TileTrees
└─ ...
```

관련 코드:

```text
Assets/Scripts/Runtime/Tiles/
```

`CTile`은 모든 시설에서 공통으로 필요한 상태와 동작을 담당하고,  
개별 시설은 필요한 경우 자신의 행동이나 점수 계산 로직을 override합니다.

### 구현 방식

`CTile`은 여러 시설에서 공통으로 필요한 상태와 동작을 담당하고,  
시설마다 달라지는 행동과 점수 계산은 각 하위 클래스에서 override하도록 구성했습니다.

이와 함께 타일이 어떤 기능을 가지고 있는지는 `[Flags]`가 적용된 `ETileState`로 관리합니다.

```text
ETileState
├─ Built       : 건설된 타일
├─ Road        : 도로 건설 대상
├─ Action      : 별도의 Tile Action을 가진 타일
├─ Upgradable  : 업그레이드 가능한 타일
├─ Point       : 축제 점수를 제공하는 타일
└─ PointUp     : 점수 강화 기능을 위해 추가했으나, 최종 구현에서는 사용하지 않음
```

각 타일은 필요한 특성을 조합해서 가질 수 있습니다.

```text
Tile A
└─ Built | Point

Tile B
└─ Built | Action

Tile C
└─ Built | Action | Point
```

이를 통해 `GameManager`는 특정 시설의 종류를 직접 확인하지 않고,  
타일이 필요한 기능을 가지고 있는지만 확인해 공통 처리를 수행할 수 있습니다.

예를 들어 Tile Action UI는 해당 타일의 구체적인 종류와 관계없이  
`Action` 플래그가 있는지를 확인해 표시하며, 업그레이드 역시 `Upgradable` 플래그를 기준으로 처리합니다.

반면 실제 Action이나 점수 계산 방식처럼 시설마다 내용이 달라지는 부분은  
각 `CTile` 하위 클래스에서 override해 구현합니다.

```text
                  CTile
                    │
        ┌───────────┼───────────┐
        ↓           ↓           ↓
     공통 상태    ETileState    virtual method
                    │                │
              기능 조합          시설별 override
                    │                │
              Action 여부        OnAction()
              Point 여부         OnScore()
              Upgrade 여부       ...
```

따라서 타일 시스템에서는

- **Flags**로 타일이 어떤 기능을 가지고 있는지 표현하고
- **상속과 override**로 그 기능이 실제로 어떻게 동작하는지를 구현하는

두 가지 방식을 함께 사용했습니다.

### 장점

새로운 시설을 추가할 때 기존 시설의 로직을 수정하지 않고  
새로운 `CTile` 하위 클래스에서 해당 시설의 규칙을 구현할 수 있습니다.

또한 `GameManager`가 시설의 종류를 하나씩 구분할 필요 없이  
`Action`, `Upgradable`, `Point`와 같은 기능 단위로 타일을 처리할 수 있습니다.

예를 들어 새로운 Action 타일이 추가되어도 `GameManager`에 해당 타일을 위한 별도의 조건문을 추가할 필요 없이,  
타일에 `Action` 플래그를 부여하고 필요한 `OnAction()`만 구현하면 기존 공통 처리 흐름을 그대로 사용할 수 있습니다.

이를 통해 시설이 늘어날 때마다

```text
타일 종류 확인
    ↓
switch / if
    ↓
시설별 처리
```

와 같은 분기 구조가 계속 커지는 것을 줄일 수 있었습니다.

### 프로젝트에서 배운 점

여러 객체가 같은 종류의 기본 동작을 공유하면서도 서로 다른 규칙을 가져야 할 때,  
공통 클래스에 모든 경우를 조건문으로 넣는 것보다 **공통 부분과 달라지는 부분을 구분하는 것이 중요하다**는 점을 경험했습니다.

`CTile`에는 여러 시설에서 실제로 공유하는 기능을 두고,  
점수 계산이나 Tile Action처럼 시설마다 달라지는 기능은 각 시설이 직접 구현하도록 하면서 상속과 override를 실제 게임 규칙에 적용해볼 수 있었습니다.

또한 상속을 사용한다고 해서 모든 기능을 부모 클래스에 넣는 것이 좋은 것은 아니라는 점도 알게 되었습니다.

공통으로 사용되는 기능은 `CTile`에 두되, 특정 시설에서만 필요한 규칙은 해당 시설 클래스에 남겨두는 편이 각 클래스의 역할을 이해하고 수정하기 쉬웠습니다.

이를 통해 **공통 기능은 재사용하면서도, 객체마다 다른 규칙은 각 객체가 직접 담당하도록 나누는 것이 확장에 유리하다**는 점을 배울 수 있었습니다.

## 7. Tech System

게임에는 다섯 가지 기술 분야가 존재합니다.

- 과학
- 음악
- 미술
- 체육
- 미스터리

각 분야의 기술 수치는 단순한 능력치가 아니라  
**카드의 사용 조건과 일부 타일의 효과를 결정하는 게임 상태**로 사용됩니다.

### 카드 사용 조건

카드 자체는 처음부터 데이터로 존재하지만,  
일부 카드는 특정 분야의 기술 수치를 만족해야 사용할 수 있습니다.

`GameCard`의 `TagList`에 필요한 기술 조건을 정의하고,  
카드를 사용할 때 현재 기술 상태가 해당 조건을 만족하는지 확인합니다.

```text
GameCard
   │
   └─ TagList
        │
        │ 필요한 기술 조건
        ↓
현재 기술 상태
        │
        ├─ 조건 충족 → 카드 사용 가능
        └─ 조건 미충족 → 카드 사용 불가
```

따라서 새로운 카드가 기술 레벨에 따라 새롭게 생성되거나 해금되는 구조라기보다,  
**이미 존재하는 카드 중 플레이어가 사용할 수 있는 카드의 범위가 기술 발전에 따라 넓어지는 방식**으로 동작합니다.

### 타일 효과 강화

기술 수치는 카드의 사용 조건뿐 아니라  
이미 건설된 일부 시설의 효과를 강화하는 데에도 사용됩니다.

일부 타일은 자신의 점수나 액션을 계산할 때  
현재 플레이어의 특정 분야 기술 수치를 확인합니다.

```text
기술 수준
    ↓
타일 액션 / 점수 계산
    ↓
기술 수준에 따른 효과 변화
```

예를 들어 특정 기술 수준에 도달하면  
시설이 더 넓은 범위를 대상으로 효과를 적용하거나, 기존보다 강화된 점수 계산 방식을 사용할 수 있습니다.

이처럼 기술의 변화는 단순히 숫자 하나가 증가하는 것으로 끝나지 않고,  
**기존 카드와 타일 시스템의 동작 방식에 영향을 주도록 연결되어 있습니다.**

### 장점

기술 진행도를 카드와 타일마다 개별적으로 관리하지 않고,  
공통된 기술 상태를 기준으로 판단하도록 구성했습니다.

예를 들어 하나의 과학 기술 수치는

```text
과학 기술
   ├─ 특정 과학 카드의 사용 조건
   └─ 과학 관련 시설의 강화 조건
```

처럼 서로 다른 시스템에서 함께 사용할 수 있습니다.

이를 통해 카드 사용 조건과 시설 강화가 서로 별개의 시스템처럼 동작하지 않고,  
플레이어가 선택한 분야의 성장 정도를 기준으로 자연스럽게 연결되도록 구성했습니다.

### 프로젝트에서 배운 점

이 시스템을 구현하면서 **하나의 상태값을 여러 시스템이 함께 참조하도록 만들면 서로 다른 기능을 자연스럽게 연결할 수 있다**는 점을 경험했습니다.

기술 수치는 그 자체로 별도의 복잡한 동작을 수행하기보다,  
카드와 타일이 자신의 동작을 결정할 때 참고하는 기준으로 사용됩니다.

이를 통해 기술 시스템이 카드나 타일의 구체적인 동작까지 직접 처리하지 않고도,  
각 시스템의 행동에 영향을 줄 수 있도록 구성할 수 있었습니다.

또한 하나의 진행도를 여러 시스템에서 공유하면  
같은 성장 상태를 각 기능마다 따로 관리할 필요가 줄어들고, 게임 전체의 진행 상태도 더 일관되게 유지할 수 있다는 점을 배웠습니다.

## 8. Snapshot 기반 Undo

### 문제

이 프로젝트에서는 하나의 카드 사용이나 타일 액션이 여러 게임 상태를 동시에 변경할 수 있습니다.

예를 들어 카드 한 장을 사용하면 다음과 같은 변화가 함께 발생할 수 있습니다.

```text
카드 사용
   │
   ├─ 자원 감소
   ├─ 완성도 / 관심도 / 안정도 변화
   ├─ 기술 수치 변화
   ├─ 손패에서 카드 제거
   ├─ 새로운 카드 획득
   ├─ 덱 상태 변화
   └─ 타일 건설
```

Undo를 각각의 변화에 대한 역연산으로 구현한다면,  
변경된 모든 상태에 대해 정확한 반대 동작을 별도로 구현해야 합니다.

예를 들어

```text
자본 -10
완성도 +1
카드 제거
새 카드 획득
시설 건설
```

이라는 행동을 되돌리려면

```text
자본 +10
완성도 -1
사용한 카드 복원
추가된 카드 제거
건설된 시설 제거
```

와 같이 해당 행동이 변경한 상태를 모두 추적하고 복원해야 합니다.

카드와 타일의 종류가 늘어날수록 하나의 행동이 영향을 주는 상태도 다양해지기 때문에,  
각 행동마다 별도의 Undo 로직을 구현하고 유지하는 과정에서 누락이나 실수가 발생할 가능성이 있다고 판단했습니다.

### 선택

Undo를 처음 설계할 때는 각 행동에 실행과 취소 동작을 함께 정의하는 **Command Pattern**을 고려했습니다.

```text
Command
├─ Execute()
└─ Undo()
```

이 방식은 행동 단위가 명확하고, 각 행동이 자신의 Undo 동작을 직접 관리할 수 있다는 장점이 있습니다.

하지만 이 프로젝트에서는 하나의 카드나 타일 액션이 여러 시스템의 상태를 동시에 변경하기 때문에,  
새로운 행동을 추가할 때마다 실행 로직뿐 아니라 정확한 `Undo()` 로직도 함께 작성하고 검증해야 했습니다.

특히 초기 개발 기간이 15일로 제한되어 있었고, 그 기간 안에 핵심 게임 루프를 완성하는 것을 우선했기 때문에  
각 행동의 역동작을 개별적으로 구현하는 방식은 구현과 테스트 비용이 크다고 판단했습니다.

따라서 행동 자체를 되돌리는 대신,  
**행동 직전의 게임 상태를 저장하고 필요할 때 해당 상태로 복원하는 Snapshot 방식**으로 방향을 변경했습니다.

관련 코드:

```text
Assets/Scripts/Runtime/GameManager/GameManager_Undo.cs
```

Snapshot에는 Undo에 필요한 주요 게임 상태를 저장합니다.

```text
Undo Snapshot
├─ 자원 상태
├─ 기술 상태
├─ 손패
├─ 일반 카드 덱
├─ 특수 카드 덱
├─ 카드 점수 관련 상태
├─ 건설된 타일
├─ 타일 내부 변수
└─ 타일 액션 사용 여부
```

### 구현 방식

플레이어의 행동이 실제 게임 상태를 변경하기 전에  
현재 상태를 복사하여 Undo 데이터로 저장합니다.

```text
현재 게임 상태
      ↓
Snapshot 생성
      ↓
Undo Stack에 Push
      ↓
플레이어 행동 실행
```

Undo가 실행되면 가장 최근에 저장된 Snapshot을 꺼내  
해당 시점의 게임 상태로 복원합니다.

```text
Undo 실행
    ↓
Undo Stack에서 Pop
    ↓
Snapshot으로 복구
    ↓
UI 갱신
```

게임 규칙에 영향을 주는 자원, 카드, 기술, 타일 상태는 Snapshot으로 복원하지만,  
UI 표시나 파티클처럼 현재 게임 상태를 기반으로 다시 구성할 수 있는 정보까지 모두 저장하지는 않았습니다.

실제 구현에서도 이러한 정보는 Snapshot으로 복원된 게임 상태를 기준으로 다시 갱신하도록 구성했습니다.

### 장점

Snapshot 방식에서는 Undo 시스템이 각 카드나 타일 액션의 구체적인 동작을 알 필요가 없습니다.

예를 들어 새로운 카드가 자원, 기술, 손패를 동시에 변경하더라도  
해당 상태가 Snapshot에 포함되어 있다면 카드의 효과에 맞는 별도의 Undo 로직을 추가하지 않고 이전 상태로 복원할 수 있습니다.

즉 새로운 행동이 추가될 때마다

```text
행동 구현
   ↓
전용 Undo 로직 구현
   ↓
역동작 검증
```

을 반복하기보다,

```text
행동 전 Snapshot 저장
      ↓
행동 실행
      ↓
필요하면 이전 상태 복원
```

이라는 동일한 흐름을 사용할 수 있었습니다.

또한 여러 시스템의 상태를 하나의 시점으로 함께 저장하기 때문에,  
Undo 과정에서 일부 상태만 되돌아가고 다른 상태는 남는 문제를 줄일 수 있었습니다.

현재 프로젝트 규모에서는 Snapshot으로 저장해야 하는 데이터의 양도 크지 않아,  
메모리 사용량보다 구현의 단순성과 복원 과정의 안정성을 우선하는 것이 적절하다고 판단했습니다.

### 프로젝트에서 배운 점

Undo를 구현하면서 **행동을 되돌리는 방식과 상태를 되돌리는 방식은 서로 다른 접근 방법**이라는 점을 경험했습니다.

처음 고려했던 Command Pattern은 행동 단위가 명확하다는 장점이 있었지만,  
이 프로젝트에서는 하나의 행동이 여러 시스템의 상태를 함께 변경하기 때문에 각 Command의 `Undo()`를 구현하고 검증하는 비용이 컸습니다.

반면 Snapshot 방식은 행동의 세부 내용을 Undo 시스템이 알지 않아도  
행동 이전의 상태를 기준으로 복원할 수 있어 현재 프로젝트에서는 더 단순하게 적용할 수 있었습니다.

이를 통해 **설계 방법 자체의 장단점뿐 아니라 프로젝트의 규모, 개발 기간, 구현 및 검증 비용까지 함께 고려해 방법을 선택하는 것이 중요하다**는 점을 배웠습니다.

또한 Snapshot을 구현하면서 어떤 상태를 직접 저장해야 하고,  
어떤 정보는 복원된 상태를 기반으로 다시 구성할 수 있는지도 구분할 필요가 있었습니다.

게임 규칙에 영향을 주는 상태는 Snapshot에 포함하고,  
UI나 파티클처럼 파생 가능한 정보는 복원 이후 다시 갱신하도록 구성하면서  
**상태 복원에 실제로 필요한 데이터와 다시 만들어낼 수 있는 데이터를 구분하는 것이 중요하다**는 점도 경험했습니다.

## 9. Save / Load

### 문제

게임을 저장하려면 단순한 자원 수치만 기록해서는 부족했습니다.

게임을 불러왔을 때 이전 진행 상황을 동일하게 복원하기 위해  
다음과 같은 상태를 함께 저장할 필요가 있었습니다.

- 현재 랜덤 시드
- 현재 진행 일수
- 달성한 업적
- 자원 및 생산량
- 현재 기술 상태
- 카드로 획득한 점수
- 손패
- 일반 카드 덱
- 특수 카드 덱
- 건설된 타일
- 타일별 내부 상태

특히 카드와 타일은 게임 실행 중 여러 객체와 연결되어 동작하기 때문에,  
런타임에서 사용하는 객체 전체를 저장하기보다 **게임 상태를 다시 구성하는 데 필요한 값이 무엇인지 구분할 필요가 있었습니다.**

### 선택

게임 상태를 복원하는 데 필요한 값만 `CSaveData`에 저장하고,  
이를 JSON으로 직렬화하여 파일로 기록하도록 구성했습니다.

관련 코드:

```text
Assets/Scripts/Runtime/Save/SaveManager.cs
Assets/Scripts/Runtime/Save/SaveSlotUI.cs
```

저장 과정은 개념적으로 다음과 같습니다.

```text
현재 게임 상태
      ↓
필요한 상태 추출
      ↓
CSaveData 생성
      ↓
JSON 직렬화
      ↓
파일 저장
```

불러올 때에는 저장된 데이터를 역직렬화한 뒤,  
해당 값을 바탕으로 실제 게임 상태를 다시 구성합니다.

```text
저장 파일
    ↓
JSON 역직렬화
    ↓
CSaveData
    ↓
게임 상태 복원
    ↓
UI 갱신
```

### 구현 방식

자원이나 기술 수치처럼 값 자체를 복원할 수 있는 정보는 그대로 Save Data에 기록합니다.

반면 카드와 타일처럼 런타임에서 객체로 동작하는 요소는  
객체 자체를 저장하는 대신 **어떤 객체였는지와 현재 상태를 복원하는 데 필요한 정보**를 저장합니다.

예를 들어 타일의 경우 건설된 타일의 종류와 위치, 내부 상태 등을 저장하고,  
불러올 때 해당 데이터를 기준으로 타일을 다시 구성한 뒤 저장되어 있던 상태를 적용합니다.

```text
타일 Save Data
   │
   ├─ 타일 종류
   ├─ 위치
   └─ 내부 상태
        ↓
타일 재구성
        ↓
저장된 상태 적용
```

카드 역시 손패와 덱의 현재 구성을 저장해  
불러오기 이후 동일한 카드 상태를 다시 구성하도록 했습니다.

또한 랜덤 시드도 Save Data에 포함하여  
게임 진행에 필요한 난수 관련 값을 함께 복원하도록 했습니다.

### Save Slot

하나의 저장 파일만 사용하는 대신  
여러 개의 세이브 슬롯에서 서로 다른 진행 상태를 관리할 수 있도록 구현했습니다.

게임 시작 시에는 새 게임을 시작하거나  
기존 슬롯에 저장된 진행 상태를 선택해 이어서 플레이할 수 있습니다.

### 장점

런타임 객체와 저장 데이터를 분리하면서  
저장에 필요하지 않은 객체 참조나 화면 표시 상태까지 파일에 포함하지 않고, **게임을 복원하는 데 필요한 데이터만 관리할 수 있었습니다.**

또한 실제 게임 객체의 구조가 아니라 `CSaveData`를 저장 형식의 기준으로 사용하기 때문에

```text
Runtime Object
       ↕
    Save Data
       ↕
      JSON
```

처럼 게임에서 사용하는 객체와 파일에 저장되는 데이터의 역할을 구분할 수 있었습니다.

이 방식은 앞서 구현한 Undo와도 비슷하게,  
**현재 화면을 그대로 보존하는 것이 아니라 게임을 다시 구성할 수 있는 상태를 저장한다**는 기준을 유지할 수 있다는 장점이 있었습니다.

### 프로젝트에서 배운 점

Save / Load를 구현하면서 **저장한다는 것이 현재 메모리의 모든 정보를 그대로 기록하는 것은 아니라는 점**을 경험했습니다.

게임을 다시 시작했을 때 동일한 진행 상태를 만들 수 있다면,  
UI나 런타임 객체의 참조처럼 다시 구성할 수 있는 정보까지 저장할 필요는 없었습니다.

따라서 어떤 데이터를 저장할지를 결정할 때도

**“현재 객체가 무엇을 가지고 있는가”보다 “게임을 다시 구성하려면 어떤 상태가 필요한가”**

를 기준으로 생각하는 것이 중요하다는 점을 배웠습니다.

또한 Save / Load는 데이터를 파일에 기록하는 것만으로 끝나는 기능이 아니라,  
저장된 데이터를 다시 카드, 타일, 자원 등 여러 시스템의 실제 상태로 연결하는 **복원 과정까지 함께 설계해야 하는 기능**이라는 점도 경험했습니다.

## 10. GameManager

### 문제

프로젝트 초기에는 제한된 개발 기간 안에서 핵심 게임 루프를 완성하는 것을 우선했습니다.

그 과정에서 자원, 턴 진행, 카드, 타일, UI 등 여러 시스템의 상태와 흐름을 `GameManager`가 함께 관리하게 되었습니다.

기능이 늘어나면서 `GameManager`의 코드도 함께 커졌고, 하나의 파일 안에서 서로 다른 역할의 코드를 찾고 수정하기 어려워지는 문제가 생겼습니다.

예를 들어 `GameManager`는 다음과 같은 기능을 함께 처리하고 있습니다.

```text
GameManager
├─ 게임 상태 관리
├─ 카드 액션 처리
├─ 턴 진행
├─ 타일 관리
├─ 일일 이벤트
├─ UI 갱신
└─ Undo
```

이 상태에서 기능을 계속 추가하면 하나의 파일이 지나치게 커지고, 특정 기능을 수정할 때 관련 코드를 찾는 비용도 증가한다고 판단했습니다.

### 선택

초기 개발 기간 안에서 기존 구조를 크게 변경하지 않으면서 코드의 위치를 기능별로 구분하기 위해  
`partial class`를 사용해 `GameManager`를 여러 파일로 나누었습니다.

```text
GameManager.cs
├─ GameManager_Action.cs
├─ GameManager_DailyEvent.cs
├─ GameManager_Tile.cs
├─ GameManager_TurnManagement.cs
├─ GameManager_UI.cs
├─ GameManager_Undo.cs
└─ GameManager_Update.cs
```

각 파일에는 이름에 해당하는 기능의 로직을 모아두어,  
같은 `GameManager` 클래스의 상태를 공유하면서도 기능별 코드를 따로 확인할 수 있도록 구성했습니다.

### 구현 방식

`partial class`를 사용했기 때문에 각 파일은 별도의 객체가 아니라 하나의 `GameManager` 클래스를 구성합니다.

```text
GameManager_Action.cs
        │
GameManager_Tile.cs
        │
GameManager_UI.cs
        ├─────→ 하나의 GameManager
GameManager_Undo.cs
        │
GameManager_Update.cs
```

따라서 기존 코드에서 사용하던 필드나 메서드 접근 방식을 크게 변경하지 않고도,  
기능에 따라 코드의 물리적인 위치를 나눌 수 있었습니다.

예를 들어 타일 관련 동작을 수정할 때는 `GameManager_Tile.cs`,  
Undo 관련 동작을 수정할 때는 `GameManager_Undo.cs`를 중심으로 확인할 수 있도록 했습니다.

### 장점

기존 시스템을 대규모로 다시 작성하지 않고도  
한 파일에 모든 코드가 집중되는 문제를 줄일 수 있었습니다.

또한 기능별로 파일이 분리되면서

- 관련 코드를 찾기 쉬워지고
- 하나의 파일에서 서로 다른 기능의 코드가 뒤섞이는 정도를 줄이며
- 새로운 기능을 추가할 때 어느 파일에 작성할지 기준을 정할 수 있었습니다.

초기 개발 기간이 15일로 제한된 상황에서는 전체 구조를 다시 설계하는 것보다,  
기존 동작을 유지하면서 코드 탐색과 수정 비용을 줄일 수 있는 방법이 더 적합하다고 판단했습니다.

### 한계

`partial class`는 **코드를 여러 파일로 나눌 뿐, 클래스 자체의 책임을 분리하지는 않습니다.**

현재 `GameManager`는 파일이 나뉘어 있더라도 여전히 자원, 턴, 카드, 타일, UI 등 여러 시스템의 상태와 흐름을 알고 있습니다.

즉

```text
파일은 여러 개
      ↓
실제 객체는 하나
      ↓
여러 시스템의 책임이 GameManager에 집중
```

되어 있다는 한계가 있습니다.

프로젝트의 규모가 더 커지고 각 시스템의 기능이 복잡해진다면  
자원, 턴, 카드, 타일 등 독립적으로 관리할 수 있는 책임을 별도의 클래스로 분리하는 것이 다음 단계라고 생각합니다.

예를 들면 다음과 같은 방향을 고려할 수 있습니다.

```text
GameManager
   │
   ├─ ResourceManager
   ├─ TurnManager
   ├─ CardManager
   └─ TileManager
```

이 경우 `GameManager`는 각 시스템의 세부 상태를 직접 관리하기보다  
게임 전체의 진행 순서를 조정하는 역할에 더 집중할 수 있습니다.

### 프로젝트에서 배운 점

이 과정에서 **파일을 나누는 것과 객체의 책임을 나누는 것은 서로 다른 문제**라는 점을 경험했습니다.

`partial class`를 사용하면서 코드의 위치를 기능별로 정리하는 것만으로도 개발 중 코드를 찾고 수정하기는 훨씬 편해졌지만,  
클래스가 담당하는 역할 자체가 줄어드는 것은 아니었습니다.

또한 제한된 일정 안에서는 가장 이상적인 구조로 한 번에 리팩터링하는 것보다,  
현재 문제를 줄이면서도 기존 기능을 안정적으로 유지할 수 있는 방법을 선택하는 것이 더 현실적일 수 있다는 점도 배웠습니다.

현재 구조는 초기 개발 단계에서는 충분히 관리할 수 있었지만,  
기능이 더 증가한다면 **코드의 물리적인 분리에서 끝나지 않고 실제 책임 단위의 분리까지 진행해야 한다**고 생각합니다.

## 11. 구현 과정에서 중요하게 생각한 점

### 기능 완성 우선

이번 프로젝트는 제한된 개발 기간 안에서  
실제로 처음부터 끝까지 플레이할 수 있는 게임을 만드는 것을 목표로 했습니다.

따라서 처음부터 모든 구조를 이상적인 형태로 완성하려 하기보다,

```text
핵심 기능 구현
 ↓
실제 플레이
 ↓
문제 발견
 ↓
기능 확장
 ↓
필요한 부분 리팩터링
```

순서로 개발했습니다.

기능을 추가하는 것 자체보다  
**게임의 전체 흐름이 실제로 연결되어 동작하는 상태를 먼저 만드는 것**을 우선했습니다.

### 반복되는 부분과 고유한 동작의 분리

콘텐츠가 계속 추가되는 구조에서는 반복되는 부분을 공통화하면서도 각 콘텐츠의 고유한 규칙을 따로 표현할 수 있도록 구성했습니다.

카드는 이름, 비용, 사용 조건과 같은 반복되는 정보를 `ScriptableObject` 데이터로 관리하고, 실제 효과의 실행은 코드에서 처리했습니다.

타일은 여러 시설에서 공통으로 사용하는 기능을 `CTile`에 두고, 시설마다 달라지는 액션과 점수 계산은 각 하위 클래스에서 구현했습니다.

이를 통해 반복되는 코드를 줄이면서도, 카드나 시설마다 필요한 고유한 동작을 별도로 구현할 수 있도록 했습니다.

### 시스템 간 상태 일관성

게임의 여러 기능은 서로 밀접하게 연결되어 있습니다.

예를 들어 카드 한 장의 사용이

```text
자원
기술
손패 / 덱
타일
점수
```

등 여러 상태를 동시에 변경할 수 있습니다.

따라서 개별 기능이 정상적으로 동작하는 것뿐 아니라 **여러 시스템이 동일한 게임 진행 상태를 일관되게 유지하는 것**도 중요했습니다.

특히 Undo와 Save / Load를 구현하면서 어떤 상태를 저장하고 어떤 상태를 다시 구성해야 하는지를 구분하는 데 이 부분을 많이 고려했습니다.

### 요구사항에 맞는 구조 선택

구현 방법을 결정할 때 특정 설계 방식이나 패턴을 사용하는 것 자체를 목표로 두지는 않았습니다.

예를 들어 Hex Grid에서는 반경 2 이상의 범위 탐색이 필요했기 때문에 Cube Coordinate System을 사용했고,  
Undo는 처음 고려했던 Command Pattern보다 현재 프로젝트 규모에서 구현과 검증이 단순한 Snapshot 방식을 선택했습니다.

또한 `GameManager` 역시 초기 개발 단계에서는 전체 구조를 다시 분리하기보다  
`partial class`를 이용해 기능별 코드를 먼저 정리했습니다.

이 과정을 통해 **구현 방법의 장단점뿐 아니라 실제 요구사항, 프로젝트 규모, 개발 기간을 함께 고려해 선택하는 것이 중요하다**고 생각했습니다.

## 12. 현재 구조에서 개선하고 싶은 부분

현재 구조는 초기 개발 범위에서 게임을 완성하는 데에는 충분했지만,  
기능이 계속 추가되고 프로젝트 규모가 커진다면 개선이 필요한 부분도 있습니다.

앞에서 설명한 구조의 한계를 기준으로 다음 부분을 우선적으로 개선하고 싶습니다.

### 1. GameManager의 책임 분리

가장 우선적으로 개선하고 싶은 부분은 `GameManager`에 집중된 책임입니다.

앞에서 설명했듯 현재는 `partial class`로 파일을 나누었지만,  
실제로 담당하는 역할 자체가 줄어든 것은 아닙니다.

프로젝트 규모가 더 커진다면 자원, 턴, 카드, 타일처럼 독립적으로 관리할 수 있는 책임을 분리하고,  
`GameManager`는 **게임 전체의 진행 순서를 조정하는 역할**에 집중하도록 개선하고 싶습니다.

### 2. Save Version에 따른 데이터 변환

현재 Save Data에는 버전 정보가 존재하지만,  
저장 구조가 변경되었을 때 이전 버전의 데이터를 현재 구조로 변환하는 기능까지는 구현하지 않았습니다.

초기 버전에서는 큰 문제가 없지만, 실제 배포 이후 업데이트가 계속되면  
필드가 추가되거나 저장 방식이 변경될 가능성이 있습니다.

이 경우 이전 저장 데이터를 바로 읽는 것보다

```text
저장 데이터 로드
      ↓
Save Version 확인
      ↓
이전 형식인 경우 필요한 값 변환
      ↓
현재 Save Data로 복원
```

과 같은 과정을 추가하는 것이 필요합니다.

향후 Save 구조가 실제로 변경되는 시점에는  
기존 플레이어의 진행 상황을 유지할 수 있도록 버전별 변환 처리를 추가하고 싶습니다.

## 13. 개발 과정

프로젝트는 하나의 완성된 구조를 처음부터 만든 것이 아니라  
게임을 실제로 플레이하면서 기능을 순차적으로 확장했습니다.

대략적인 개발 흐름은 다음과 같습니다.

```text
Hex Grid (육각 좌표계)
 ↓
타일 생성 및 배치
 ↓
자원 시스템과 턴 시스템
 ↓
카드 및 기술
 ↓
손패와 덱
 ↓
카드를 통한 타일 건설
 ↓
타일 액션
 ↓
카메라 / 이펙트 / UI 개선
 ↓
저장 / 불러오기
 ↓
프롤로그와 엔딩
 ↓
Undo
 ↓
일일 이벤트
 ↓
업적
```

각 단계에서 이전 시스템과 새 기능을 연결하면서  
게임 전체 상태가 정상적으로 유지되는지 확인하는 방식으로 개발했습니다.

## 14. 프로젝트를 통해 얻은 경험

이 프로젝트를 진행하면서 개별 기능을 구현하는 것뿐 아니라,  
여러 시스템이 연결된 게임을 하나의 상태로 관리하는 과정을 경험했습니다.

특히 다음과 같은 문제를 직접 다뤘습니다.

- 반복해서 추가되는 카드 데이터를 관리하고 실제 실행 로직과 연결하는 구조
- Hex Grid에서 인접 타일뿐 아니라 반경 단위의 범위를 탐색하는 방법
- 공통 기능을 공유하면서도 시설마다 서로 다른 행동과 점수 계산을 구현하는 구조
- 카드 사용으로 자원, 기술, 손패, 덱, 타일, 점수 등 여러 상태가 함께 변경되는 과정의 관리
- 행동 이전의 게임 상태를 저장하고 복원하는 Undo
- 게임을 다시 구성하는 데 필요한 상태를 Save Data로 저장하고 복원하는 Save / Load
- 기능이 증가하면서 커진 `GameManager`를 기존 동작을 유지한 채 점진적으로 정리하는 과정
- 초기 개발 기간 안에서 필요한 기능의 완성과 구조 개선 사이의 우선순위를 판단하는 과정

이 과정에서 특정 기술이나 설계 방식을 사용하는 것 자체보다,  
**현재 필요한 기능과 연산이 무엇인지 파악하고 프로젝트의 규모와 상황에 맞는 구현 방법을 선택하는 것**이 중요하다는 점을 경험했습니다.

## 15. 마무리

이 프로젝트에서는 하나의 기능이나 기술을 보여주는 데모가 아니라,  
여러 시스템이 서로 연결되어 처음부터 끝까지 실제로 플레이할 수 있는 게임을 만드는 것을 목표로 했습니다.

```text
카드를 뽑는다
 ↓
자원을 관리한다
 ↓
카드를 사용한다
 ↓
시설을 건설한다
 ↓
배치와 타일 액션을 활용한다
 ↓
기술을 발전시킨다
 ↓
턴을 진행한다
 ↓
게임을 저장하고 이어서 플레이한다
 ↓
최종 결과를 확인한다
```

이를 위해 Hex Grid와 타일 시스템을 시작으로  
자원과 턴, 카드와 기술, Undo, Save / Load 등의 기능을 순차적으로 추가하고 하나의 게임 흐름으로 연결했습니다.

현재 구조에는 초기 개발 과정에서 선택한 방식으로 인해 개선할 수 있는 부분도 남아 있습니다.  
다만 현재 구조의 한계를 파악하고 있으며, 프로젝트가 확장될 경우 어떤 책임을 분리하고 어떤 부분을 보완해야 하는지도 함께 검토했습니다.

이 프로젝트를 통해 **개별 기능의 구현뿐 아니라 여러 시스템을 연결하고, 실제로 동작하는 하나의 프로그램으로 완성하는 과정**을 경험할 수 있었습니다.

[← README로 돌아가기](./README.md)